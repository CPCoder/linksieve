# LinkSieve

LinkSieve is a browser extension for filtering LinkedIn jobs based on
their external application URLs.

The extension currently supports:

- Chrome / Chromium
- Firefox

LinkSieve operates exclusively on LinkedIn job pages.

## Development

Requirements:

- Node.js
- npm

Install dependencies:

```bash
npm install